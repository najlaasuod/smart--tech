<?php require_once '../config/database.php'; require_once '../includes/functions.php'; require_admin();
if(isset($_POST['status'])){ $pdo->prepare('UPDATE orders SET status=? WHERE id=?')->execute([$_POST['status'],(int)$_POST['id']]); flash('success','تم تحديث الطلب'); redirect('orders.php'); }
$orders=$pdo->query('SELECT o.*, u.name user_name FROM orders o JOIN users u ON u.id=o.user_id ORDER BY o.id DESC')->fetchAll(); include '../includes/header.php'; ?>
<h2>إدارة الطلبات</h2><table class="table"><tr><th>#</th><th>العميل</th><th>الإجمالي</th><th>الحالة</th><th>تحديث</th></tr><?php foreach($orders as $o): ?><tr><td><?= $o['id'] ?></td><td><?= e($o['user_name']) ?></td><td>$<?= e($o['total']) ?></td><td><?= e($o['status']) ?></td><td><form method="post" class="d-flex gap-2"><input type="hidden" name="id" value="<?= $o['id'] ?>"><select name="status" class="form-select form-select-sm"><option>new</option><option>processing</option><option>completed</option><option>cancelled</option></select><button class="btn btn-sm btn-info">حفظ</button></form></td></tr><?php endforeach; ?></table>
<?php include '../includes/footer.php'; ?>
